# くりとみかん 記事パイプライン 確定仕様

株式会社くり房（ブランド: くりとみかん、kuri-mikan.jp）のSEO記事パイプライン。
このファイルが仕様の正本です。プロンプトの正本は `kurimikan_prompt_suite_v1.md`。
矛盾したときはこのファイルが優先されます。

このリポジトリは nortiq-pipeline（別サイト向けの同系パイプライン）から分岐したものです。
承認フロー、デッドマンスイッチ、トリップワイヤ、重複ゲート、コスト計上といった
運転の骨格はそのまま引き継ぎ、公開先と記事の中身に関わる部分を作り替えています。

---

## 1. この案件で記事を書く目的

**記事は売るために書くのではなく、コレクションページを押し上げるために書きます。**

購買クエリ（「甘平 通販」「甘平 訳あり 3kg」）で上位を取るべきなのはカートのある
`/collections/甘平` であって、記事ではありません。しかし新規ドメイン（2025年2月公開）の
コレクションページは商品グリッドだけで本文がなく、グローバルナビから1本リンクが
刺さっているだけです。Googleから見て「薄くて重要でないページ」になっています。

そこで記事を束ねてコレクションを押し上げます。

```
記事「甘平とは」               ─┐
記事「甘平と紅まどんなの違い」   ─┼→ /collections/kanpei
記事「甘平の旬はいつ」           ─┤   ← ここを押し上げる
記事「甘平の保存方法」           ─┘
```

効果は2つあります。

1. **評価の受け渡し** — 内部リンクを通じてページの重要度が流れる。関連記事4本から
   刺さっているコレクションは「サイトの中心的なページ」と判定されやすい
2. **トピックの証明** — 甘平まわりの記事が束であることで「このサイトは甘平に詳しい」と
   Googleが認識し、取引クエリでもコレクションが評価されやすくなる

### 記事のKPI

**「その記事が何個売ったか」ではなく「リンク先コレクションの順位とクリックが上がったか」。**

記事単体のCVで判断すると全部失敗に見えて、正しい施策を捨てることになります。
月次戦略（P-16）はこの順で判断するよう指示してあります。

### 実装上の帰結

- `keywords.target_collection` は必須（`require_target_collection=true`）。
  狙い先の無いキーワードは、トークンを使う前に `generate.ts` が落とす
- 仕上げで本文末尾にコレクション導線を機械的に付ける（`quality/collection_link.ts`）
- アンカーテキストに品種名が入っているかを検査する。「こちら」ではリンク先が
  何のページか検索エンジンに伝わらず、評価の受け渡しが起きない
- 1つのコレクションに4〜6本を集中させる。30本をバラバラのコレクションに1本ずつでは
  何も動かない。運用の判断は P-16 が担う

---

## 2. 絶対ルール（変更禁止）

- **自動公開は存在しない。**（既定 `full_auto_publish=false`）公開トリガは「承認者が実記事を
  読んで承認ボタンを押す」のみ。全記事 `status=approval_pending` で承認キューへ。
  この設定を外して自動投稿へ進む手順はセクション9
- **フェイルクローズド。** 公開予定時刻から72時間超の未公開分は保留に戻す。
  代表不在時は新規公開ゼロが正しい
- **judge不一致は人間エスカレーション。**（既定時）自動棄却も自動通過もしない。
  `judge_disagreement` フラグ付きで承認キューへ
- **法令ゲートは全自動でも止まる。** 下記セクション3を参照。ここだけは
  `full_auto_publish=true` の例外にならない
- **SelfHealingCoder は初期スコープ外。**（`self_healing_enabled=false`）適用はすべて人間承認
- **保護ファイル**（CODEOWNERS参照）はいかなる自己改修・リファクタリングでも変更しない

### 全自動公開について

`full_auto_publish=true` にすると、発案トピックを自動でqueuedにし、生成記事を
品質ゲートの結果に関わらず自動承認して公開予定に載せます。速度優先で
「まず市場に出て参加する」ための設定です。`false` に戻せば元の挙動に完全復帰します。

**この設定でもバイパスされないもの**: 法令ゲート、トリップワイヤ（halt/throttle）、
デッドマンスイッチ、重複ゲート、コレクション導線の必須化。

---

## 3. 法令ゲート（この案件で最も重要な追加）

生鮮食品の通販なので、Googleの順位下落より深刻なリスクがあります。
**順位が落ちるだけのGoogleと違い、こちらは販売者（株式会社くり房）に行政指導が来ます。**

`packages/pipeline/src/quality/compliance_gate.ts` が、本文・タイトル・
メタディスクリプションを決定論（LLM不使用）で検査します。

| 区分 | 根拠 | 例 |
|---|---|---|
| 効能効果の標榜 | 薬機法68条 | 免疫力アップ、風邪予防、疲労回復、デトックス、便秘解消、血圧を下げる、アンチエイジング、美肌効果、ダイエット効果 |
| 健康保持増進効果の誇大表示 | 健康増進法65条 | 健康になれる、医者いらず、病気にならない |
| 根拠のない最上級（優良誤認） | 景品表示法5条1号 | 日本一、最高級、最高品質、No.1、他産地を圧倒 |
| 断定・保証 | 景品表示法 | 絶対においしい、誰が食べてもおいしい、はずれがない |
| 栽培方法の表示 | 特別栽培農産物に係る表示ガイドライン | 無農薬、減農薬、無化学肥料 |
| 有機の表示 | JAS法 | 有機栽培、オーガニック（認証なし） |

判定を決定論にしている理由:

- LLMの判定は揺れる。同じ本文が日によって通ったり落ちたりする状態で
  「法令チェック済み」とは言えない
- 落とすべき表現はほぼ定型句なので、字面で十分に捕まる
- APIコストと実行時間がゼロなので、本文が変わるたびに何度でも掛け直せる

字面をすり抜けた言い換え（「体の中からきれいに」など）は P-04 の `risk_areas` で
拾わせています。二重にかけているのは、どちらか片方では抜けるためです。

### 例外の入れ方

根拠を示せる表現は `compliance_allowlist`（config）に登録します。
検出箇所の前後30字にその文字列が含まれていれば見逃します。
例: 受賞歴の出典を併記したうえでの最上級表現。

`tier1_allowed_keys` にこのキーは入れていません。自己改修で緩められないようにするためです。

---

## 4. 公開先: Shopify

- 記事URLは `/blogs/<blog>/<article>` に固定される（Shopifyはフラットな `/<handle>` を許さない）
- SEOのtitle/descriptionは記事フィールドではなく metafield（`global.title_tag` /
  `global.description_tag`）で渡す
- 本文はHTML文字列。パイプラインはmarkdownで持つので変換層を挟む
  （`site_integration/shopify/markdown_to_html.ts`）
- 記事の構造化データはテーマ側の実装が必要（articleテンプレートにJSON-LD）

### ブログを2つに分ける

| URL | 用途 |
|---|---|
| `/blogs/news` | お知らせ（既存）。発送情報、出荷開始のお知らせ |
| `/blogs/column` | SEO記事（新設）。`scripts/shopify/bootstrap_blog.ts` で作成 |

分ける理由:

- blogごとに `templateSuffix` でテンプレートを変えられる。SEO記事側だけ
  構造化データ・パンくず・末尾のコレクション誘導ブロックを持つ専用レイアウトにできる
- タグ絞り込みが blog 単位
- お知らせに読み物が混ざると、発送情報のような「今すぐ知りたい情報」が埋もれる

### 記事の同一性

- `articles.slug` が Shopify の `handle`
- `articles.shopify_article_id` が Shopify の記事GID。これを持っていれば
  `articleCreate` ではなく `articleUpdate` を呼ぶ
- handle を変えるときは `redirectNewHandle: true`。張らないと被リンクと
  既存の順位を捨てることになる

### 改修（revision）の扱い

nortiq版は公開先が静的サイトのgitリポジトリで、本文の正はサイト側にありました。
Shopify公開ではDBが本文の正になるので、改修の設計が変わります。

- 改修案は **slug を持たない**。slugは公開中の記事が握ったままにする。
  先にslugを奪う設計にすると、改修が承認されないまま公開中の記事のURLが宙に浮く
- 改修案は `articles.revision_of` で対象を指す
- 公開に成功した時点で「公開中の座」（slug と shopify_article_id）を改修案へ移し、
  旧行を `retired` にする

---

## 5. 記事の分類

### クラスタ（`keywords.cluster`）

| 値 | 内容 | 配分の初期値 |
|---|---|---|
| citrus_variety | 品種そのもの（甘平とは、南柑20号の特徴） | 45 |
| eating | 食べ方、保存、むき方 | 20 |
| gift | 贈答、のし、予算 | 15 |
| growing | 畑と栽培、産地 | 12 |
| chestnut | 栗 | 8 |

購買クエリに近いのは品種名です。ここが最大でないと、記事が増えても
押し上げたいコレクションに評価が集まりません。

### 記事タイプ（`keywords.article_type`）

`P-03a`〜`P-03g` の追加指示に対応します。

| 値 | プロンプト | 内容 |
|---|---|---|
| howto | P-03a | 保存方法、むき方、選び方 |
| comparison | P-03b | 品種比較 |
| pricing | P-03c | 価格・相場・予算 |
| grower | P-03d | 生産者・畑・栽培（一次情報が主役） |
| gift | P-03e | ギフト・贈答マナー |
| season | P-03f | 旬・収穫時期 |
| recipe | P-03g | レシピ・食べ方 |
| market_report | （なし） | 注文データ由来の相場記事。既定で凍結 |

---

## 6. Googleのリスク: scaled content abuse

2026年3月のコアアップデートで最優先の取り締まり対象として名指しされました。
編集レビューなしでAIページを大量公開していたサイトは50〜80%のトラフィック減。

判定基準は**方法ではなく価値の有無と意図**です。AI・人間・自動化のどれで作ったかは
問いません。明確にアウトなのは「キーワードリストからAI下書きを生成し、無審査で
1日30本自動公開、内容は上位記事の焼き直しで独自の経験もデータもない」状態です。

このパイプラインが正しい側にいる理由:

1. **記事ごとに人間承認が必須**（`full_auto_publish=false`）。これが決定的な部分
2. **公開ペースは週2本（月8本相当）から**。新規ドメインで月20本は編集体制と
   釣り合わず不自然
3. **一次情報を1記事に必ず1個入れる** ← ここが唯一かつ最大の分岐点

### 一次情報が枯れたら記事を止める

畑の写真、実際の収穫日、糖度の実測値、その年の天候、生産者の言葉。
競合のAI記事が絶対に持てないものが現地に無限にあります。

一次情報バンク（`primary_info_assets`）が枯れたら、記事を増やす前に
一次情報を集めるのが正しい順番です。P-16 にそう判断させています。
収穫に関する実測値は原則1シーズンで `retire` または `update`（P-18b）。
「昨年の糖度」を今年の記事に使うと事実と違う記述になります。

---

## 7. 季節性

柑橘は10月から3月、栗は秋がピークです。SEOの立ち上がりは3〜6か月かかるので、
**ピークの2〜3か月前に記事が出て、インデックスされ、順位が付いている必要があります。**

今シーズンの売上を作るのはSEOではありません。SEOは「来年の資産」として扱います。
月次戦略（P-16）は今が何月かを見て、次に厚くすべき品種を決めます。

---

## 8. 運用の入口

| コマンド | 用途 |
|---|---|
| `npm run shopify:bootstrap-blog` | `/blogs/column` を作成（初回に1度だけ） |
| `npm run seed:prompts` | プロンプト集を `prompts` テーブルへ投入 |
| `npm run seed:config` | `pipeline_config` 初期値 + authors 投入 |
| `npm run propose:keywords` | トピック発案（承認したものだけが記事化される） |
| `npm run generate:queue` | 承認済みキーワードから記事生成 |
| `npm run refit:batch` | 公開済み記事の改修 |
| `npm run dedup:sweep` | 公開前バックログの重複掃除 |

代表がやることは6つだけです（プロンプト集 付録A）。

1. 承認キューで記事を読んで承認する（公開の唯一のトリガ）
2. 一次情報を入れる
3. トピック提案の承認（どのコレクションを押し上げるかを決める）
4. 内部リンク承認キュー（公開済み記事の書き換えになるため）
5. トリップワイヤ（halt / throttle）の解除
6. 法令ゲートで止まった記事の判断


---

## 9. 自動投稿の設計

承認者は生産者ではなく弊社。ここから開く穴と、その塞ぎ方。

### 検証の粒度を「記事」から「一次情報」へ移す

弊社は法令表現も文章の質も内部リンクも検証できる。**できないのは産地の事実だけ**で、
収穫日も糖度も今年の天候も、くり房にしか分からない。記事を1本ずつ読んでも、
価値の源泉である部分は確かめられない。

そこで確認の単位を変える。一次情報を受け取る時点で1回だけ確認し、以降の記事は
その検証済み素材からしか事実を引かない。素材にない数値を書いたら機械で落とす
(全自動時は全記事に合議を掛ける)。これで記事ごとの人手が要らなくなる。

```
くり房がお知らせを書く (いまと同じ。負担ゼロ)
   ↓ 抽出
一次情報の候補 (収穫日・糖度・天候)
   ↓ 弊社が確認 ← 唯一の人手。数字と日付だけ
検証済み素材 (valid_until つき)
   ↓
記事の自動生成 → 各ゲート → 自動公開
```

### 実行の分担

| 時刻 | 実行主体 | 内容 | LLM |
|---|---|---|---|
| JST 6:00 | GitHub Actions `cron-daily` | GSC同期・順位監視・期限切れ素材の印・トリップワイヤ・デッドマン | 不要 |
| JST 7:00 | ルーチン (claude.ai/code/routines) | 発案 → リサーチ → 生成 → 自動承認 → ゲート引き継ぎ → SEO監視 | サブスク |
| 毎時 | GitHub Actions `cron-hourly` | 公開ワーカ (Shopify) | 不要 |

LLMはサブスク実行 (`LLM_BACKEND=bridge`) なのでAPIコストはゼロ。詳細は
[docs/ROUTINES.md](docs/ROUTINES.md)。**安全装置はActions側にあるので、
ルーチンが止まってもトリップワイヤ・デッドマン・公開ワーカは動き続ける。**

### 一次情報の鮮度

柑橘は年ごとに出来が変わる。去年の糖度を今年の記事に載せると事実と違う記述になり、
全自動では誰も気づかない。3段で守る。

1. `primary_info_assets.valid_until` を過ぎた素材は、そもそも記事に選ばれない
   (`listActiveAssetsByCluster`)
2. 期限切れには `refresh_needed` が立つ (cron-daily、決定論)。代表が測り直す対象を一覧できる
3. `article_assets` に記事↔素材の紐付けが残るので、期限切れから記事を逆引きして
   改修できる (`npm run refit:batch -- --stale`)

その年の収穫に関する実測値には、必ず `valid_until` を入れること。

### 自動化は段階的に上げる

| 段 | 設定 | 自動化の範囲 | 卒業条件 |
|---|---|---|---|
| 0 | `full_auto_publish=false` | なし。全記事を弊社が読む | 10本公開。法令ゲートの誤検知ゼロ、事実誤りゼロ |
| 1 | `full_auto_publish=true` + `auto_approve_scope="no_primary_info"` | 一次情報を使わない記事だけ | 20本。順位が動き始める |
| 2 | `auto_approve_scope="all"` | 検証済み素材つきの記事も | 期限切れ素材の巡回が回っている |
| 3 | (発案の自動キュー) | トピック承認も自動 | — |

**最後まで人に残るのは「どの品種を押すか」だけ。**それはビジネス判断なので自動化しない。

全自動でも例外なく止まるもの: 法令ゲート、重複ゲート、狙い先コレクションの必須化、
トリップワイヤ (halt/throttle)、デッドマン。

---

## 10. nortiq-pipeline から変えたところ

| 領域 | nortiq版 | この案件 |
|---|---|---|
| 公開先 | 静的サイトのgitリポジトリへコミット | Shopify Admin GraphQL API |
| 本文の正 | サイト側の `content/blog/*.md` | DB（`articles.body_mdx`） |
| 内部リンクの狙い先 | CVページ（/diagnostic 等） | コレクション（/collections/品種） |
| 改修の同一性 | slug | `revision_of`（slugは公開中の記事が握る） |
| 法令チェック | なし | 決定論の法令ゲート（薬機法・景表法ほか） |
| プロンプトのv3パッチ層 | 実行時に差分適用 | 廃止（プロンプト集が最初から確定版） |
| 記事の分類 | Web制作のクラスタ・記事タイプ | 柑橘ECのクラスタ・記事タイプ |

`packages/shared/fixtures/llm/` のフィクスチャは nortiq 側の題材のまま残しています。
テストダブルであり、検証している内容（配線、リトライ、しきい値）は題材に依存しません。
差し替えると、実測値に基づくしきい値のテスト（`duplicate_gate.test.ts` の 0.833 / 0.45 など）を
書き直すことになり、そこに埋め込まれた回帰知識を失います。
